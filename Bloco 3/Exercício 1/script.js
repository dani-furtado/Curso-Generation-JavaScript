var botao = document.getElementById("botao2");

// Adicionando um evento de clique ao botão
botao.addEventListener("click", function() {
  // Seu código a ser executado quando o botão for clicado
  window.alert("want to see?");
});